# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code — project settings are shared (.claude/settings.json),
# personal overrides are not.
.claude/settings.local.json

# Environment
.env
.env.*
!.env.example

# cmi5 packaging — build artefacts of a build artefact.
# The zip is ~1.5 MB and would be re-uploaded whole on every content change.
dist-cmi5
packages
*.zip
